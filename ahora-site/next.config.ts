// next.config.ts
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Optional: Whitelist domains if you use external images in your entries
  // images: {
  //   remotePatterns: [
  //     { protocol: 'https', hostname: 'pbs.twimg.com' },
  //   ],
  // },
};

export default nextConfig;